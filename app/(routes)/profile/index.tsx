import React from 'react';
import ProfileScreen from '@/screens/profile/ProfileScreen'; // Import the ProfileScreen component

export default function Profile() {
    return (
        <ProfileScreen />
    );
}
